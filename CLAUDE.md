# Claude Code 指南

## 项目

**铝型材设计师**：内置标准铝型材、连接件、板材参数库，根据用户描述和图片设计铝型材 CAD，支持导出加工参数、连接件参数、DIY 组装指导和效果图

## RCSW 工作流

本项目使用认知敏捷方法论，遵循 `Real → Cog → Spec → Work` 四阶段工作流：

1. **Real（现实约束）**：定义项目必须遵守的硬性约束（安全、兼容性、精度等），文件 `.42cog/real.md`
2. **Cog（认知模型）**：定义核心实体、唯一编码、分类和信息流，文件 `.42cog/cog.md`
3. **Spec（产品规格）**：基于 Real + Cog 生成具体的功能规格和技术方案，文件 `.42cog/pm/` 目录下
4. **Work（实施）**：按 Spec 编码实现，文件 `src/` 目录下

**原则**：每个阶段的产出是下一阶段的输入。修改上游文档时需评估对下游的影响。

## 项目结构

- **.42cog/** - 认知敏捷文档（real.md、cog.md、meta.md 及各阶段产出）
- **docs/** - 文档
- **notes/** - 笔记和草稿
- **sources/** - 参考资料
- **src/** - 源代码

## 环境

- **设备**：Apple Silicon Mac
- **Node.js**：通过 bun 管理
- **框架**：Next.js 16

## 规则

- 文档使用中文，文件名使用英文
- 不自动提交，除非用户明确要求
- 敏感信息存 `.env.local`，不入 Git
